{
  "name": "@4ks/web",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "dependencies": {
    "@4ks/api-fetch": "workspace:*"
  }
}
