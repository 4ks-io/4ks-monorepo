import React from 'react';
import { PageLayout } from '../Layout';

const Settings = () => {
  return (
    <PageLayout>
      <h2>Settings</h2>
    </PageLayout>
  );
};

export { Settings };
export default { Settings };
